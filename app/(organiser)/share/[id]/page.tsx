// TODO: Share page — OG image, copy link
export default function SharePage({ params }: { params: { id: string } }) {
  return <div className="p-4 font-sans text-ink">Share {params.id} — coming soon</div>
}
