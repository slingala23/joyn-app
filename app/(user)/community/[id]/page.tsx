// TODO: Community detail — events, members, join
export default function CommunityPage({ params }: { params: { id: string } }) {
  return <div className="p-4 font-sans text-ink">Community {params.id} — coming soon</div>
}
