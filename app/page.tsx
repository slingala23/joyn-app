// Root page — middleware redirects to /discover (user) or /dashboard (organiser)
// This page is never shown in practice but satisfies Next.js routing.
export default function RootPage() {
  return null
}
